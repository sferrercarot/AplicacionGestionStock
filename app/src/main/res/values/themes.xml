<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema base -->
    <style name="Base.Theme.AplicacionGestionStockImprenta"
        parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Paleta principal -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/onPrimary</item>

        <!-- Paleta secundaria -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/onSecondary</item>

        <!-- Barra de estado y navegación -->
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/primary</item>
    </style>

    <!-- Tema de la app -->
    <style name="Theme.AplicacionGestionStockImprenta"
        parent="Base.Theme.AplicacionGestionStockImprenta" />

    <!-- Estilo para botones primarios -->
    <style name="PrimaryButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@color/onPrimary</item>
        <item name="android:textStyle">bold</item>
        <item name="cornerRadius">16dp</item>
    </style>

</resources>
